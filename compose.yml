services:
  web:
    build: ./ui
    ports:
      - "8080:8080"
    volumes:
      - ./ui:/app
    environment:
      - ENVIRONMENT=development
    command: uvicorn app:app --host 0.0.0.0 --port 8080 --reload
